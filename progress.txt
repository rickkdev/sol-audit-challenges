# Ralph Progress Log
## Codebase Patterns
- Python CLI code uses a `src/sol_audit_challenges/` package layout with Typer commands exposed through the `sol-audit-challenges` console script in `pyproject.toml`.
- Use `.venv/bin/python -m pytest` after installing `.[dev]` locally to run the pytest suite against the editable package.
- JSON schema validation lives in `src/sol_audit_challenges/validation.py`; CLI validation commands should report both the input file path and a `#/...` schema location for failures.
- Prepared case outputs are split into `<output-dir>/public/<case-id>/source` for distributable source snapshots and `<output-dir>/private/` for draft oracle files.
- Sanitization lives in `src/sol_audit_challenges/sanitize.py`; replacement reports should include labels, replacement values, counts, and file paths, but never the sensitive `find` strings from sanitizer config.
- Bundling lives in `src/sol_audit_challenges/bundle.py`; public archives are deterministic `tar.gz` files under `bundles/<case-id>.tar.gz`, contain files below a `source/` prefix, and update public manifests with relative package paths plus SHA-256 checksums.

Started: Thu Jun  4 14:24:50 CEST 2026
---
## 2026-06-04 14:44:03 CEST - US-001
- Implemented a Python 3.12 CLI project skeleton with package metadata, Typer console entrypoint, version command, and pytest scaffolding.
- Files changed: `pyproject.toml`, `src/sol_audit_challenges/__init__.py`, `src/sol_audit_challenges/cli.py`, `tests/test_cli.py`, `.gitignore`, `README.md`, `prd.json`, `progress.txt`, `AGENTS.md`.
- **Learnings for future iterations:**
  - Patterns discovered: CLI commands should be added under `src/sol_audit_challenges/cli.py`; the console script is configured in `pyproject.toml`.
  - Gotchas encountered: Typer collapses a single-command app unless an explicit callback is present, so keep the root callback when adding subcommands.
  - Useful context: Install with `python -m pip install -e ".[dev]"` before running `.venv/bin/python -m pytest`.
---
## 2026-06-04 14:46:39 CEST - US-002
- Implemented public challenge manifest and private oracle validation commands backed by the existing JSON schemas.
- Files changed: `src/sol_audit_challenges/cli.py`, `src/sol_audit_challenges/validation.py`, `tests/test_cli.py`, `README.md`, `prd.json`, `progress.txt`, `AGENTS.md`.
- **Learnings for future iterations:**
  - Patterns discovered: Keep schema validation logic in `src/sol_audit_challenges/validation.py` and call it from thin Typer commands.
  - Gotchas encountered: Validation failures should include the user-provided file path and a JSON Schema path like `#/properties/id/pattern`.
  - Useful context: The public manifest example at `examples/challenge-manifest/case-0000.public.json` is valid and can be reused in CLI tests.
---
## 2026-06-04 14:49:33 CEST - US-003
- Implemented `prepare-case` to export a requested Git commit with `git archive`, write the clean source snapshot under the public case directory, and create a schema-valid draft private oracle outside the public tree.
- Files changed: `src/sol_audit_challenges/prepare.py`, `src/sol_audit_challenges/cli.py`, `tests/test_cli.py`, `README.md`, `prd.json`, `progress.txt`, `AGENTS.md`.
- **Learnings for future iterations:**
  - Patterns discovered: Keep Git export behavior in a focused helper module and keep Typer commands thin.
  - Gotchas encountered: Draft private oracle commits should be resolved to full hex SHAs so they satisfy `schemas/private-oracle.schema.json`.
  - Useful context: Tests can create temporary local Git repositories and validate generated private oracle files through the CLI.
---
## 2026-06-04 14:52:57 CEST - US-004
- Implemented `sanitize-case` with default removal rules for README/docs/audits/deployments/build artifacts/exploit clues, optional JSON-configured removals, configurable string replacement rules, and a public-safe sanitizer report.
- Files changed: `src/sol_audit_challenges/sanitize.py`, `src/sol_audit_challenges/cli.py`, `tests/test_cli.py`, `README.md`, `prd.json`, `progress.txt`, `AGENTS.md`.
- **Learnings for future iterations:**
  - Patterns discovered: Keep sanitization behavior in `src/sol_audit_challenges/sanitize.py` and keep Typer commands as thin error-handling wrappers.
  - Gotchas encountered: Root-level glob matching needs explicit patterns such as `README.*` and `docs/**`; `**/README.*` alone does not cover root files with `fnmatch`.
  - Useful context: Sanitizer reports intentionally omit replacement `find` strings so public reports do not reveal protocol names, incident names, addresses, or transaction hashes from private config.
---
## 2026-06-04 14:56:41 CEST - US-005
- Implemented `bundle-case` for deterministic public `tar.gz` archives, public manifest creation/updating, SHA-256 package checksums, and exclusion of private oracle files, local notes, `.git` metadata, reports, and run outputs.
- Files changed: `src/sol_audit_challenges/bundle.py`, `src/sol_audit_challenges/cli.py`, `tests/test_cli.py`, `README.md`, `prd.json`, `progress.txt`, `AGENTS.md`.
- **Learnings for future iterations:**
  - Patterns discovered: Keep bundle/archive behavior in `src/sol_audit_challenges/bundle.py`; the Typer command should only translate options, errors, and output paths.
  - Gotchas encountered: Deterministic gzip archives need both normalized tar member metadata and a fixed gzip `mtime`.
  - Useful context: Bundle archives expose source files under `source/`, so future `run-case` extraction should expect that public workspace prefix.
---
